@model User

@{
    ViewData["Title"] = "Dashboard";
}

<div class="text-center">
    <h1 class="display-4">Welcome, @Model.FirstName!</h1>
</div>

<table class="table">
    <thead>
        <tr>
            <th scope="col">Wedding</th>
            <th scope="col">Date</th>
            <th scope="col">Guest</th>
            <th scope="col">Action</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var wedding in ViewBag.weddings)
        {
            <tr>
                <td><a href="/@wedding.WeddingId">@wedding.WedderOne & @wedding.WedderTwo</a></td>
                <td>@wedding.Date.ToString("MMM d yyyy")</td>
                <td>@wedding.AllRSVPs.Count</td>
                @if(Model.UserId == @wedding.HostUserId)
                {
                    <td><a class="text-danger" asp-controller="Home" asp-action="Delete" asp-route-weddingId="@wedding.WeddingId">Delete</a></td>
                } else {
                    var RSVPd = false;
                    foreach (var rsvp in @wedding.AllRSVPs)
                    {
                        if (rsvp.UserId == @Model.UserId)
                        {
                            RSVPd = true;
                            <td>Un-RSVP</td>
                            // WOULD MAKE A POST REQUEST TO SEND WEDDING.ID AND USER.ID, BUT DON"T KNOW HOW TO WRITE VIEW SIDE RN
                        }
                    }
                    if (RSVPd == false)
                    {
                        <td>RSVP</td>
                        // WOULD MAKE A POST REQUEST TO SEND ID AND USER.ID, BUT DON"T KNOW HOW TO WRITE VIEW SIDE RN
                    }
                }
            </tr>
        }
    </tbody>
</table>

<a href="/new" class="btn btn-secondary" role="button">+ New Wedding</a>